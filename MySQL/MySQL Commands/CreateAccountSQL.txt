// 09.30.2014;
CREATE TABLE Account (
	ID int not null,
	name char(30),
	username char(15) not null,
	password char(20) not null,
	email char(40) not null,
	primary key(ID)
);

INSERT INTO Account
(ID, name, username, password, email)
	values(0, "John", "JohnDoe123", "password1", "JohnDoe@iastate.edu");

INSERT INTO Account
(ID, name, username, password, email)
	values(1, "Jane", "JaneDoe987", "computer2", "JaneDoe@iastate.edu");

INSERT INTO Account
(ID, name, username, password, email)
	values(2, "Bob", "BobDoe456", "notepad6", "BobDoe@iastate.edu");
	
// 10.01.2014;

ALTER TABLE Account
MODIFY name VARCHAR(30);

ALTER TABLE Account
MODIFY username VARCHAR(15) NOT NULL;

ALTER TABLE Account
MODIFY password VARCHAR(20) NOT NULL;

ALTER TABLE Account
MODIFY email VARCHAR(40) NOT NULL; 

// 10.19.2014 Changes made
ALTER TABLE Account AUTO_INCREMENT=3;

ALTER TABLE Account
MODIFY ID int NOT NULL AUTO_INCREMENT;

INSERT INTO Account (name, username, password, email)
VALUES ('Tyler', 'xedrix1', 'johnston1','tjj@iastate.edu');